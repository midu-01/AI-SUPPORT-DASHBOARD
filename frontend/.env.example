# Base URL of the FastAPI backend, including the /api/v1 prefix.
# Read by the browser, so it must carry the NEXT_PUBLIC_ prefix — never put a
# secret behind this name, as the value is inlined into the client bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
